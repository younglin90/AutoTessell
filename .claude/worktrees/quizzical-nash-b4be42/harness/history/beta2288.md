# CARD BETA2288_VVV9N3_DIAG_ON (beta2288) — VVV9N #3 default ON + sliver/worst_mq guard

**target_engine**: tet
**모티프**: Klingner & Shewchuk 2008 §6 ablation — H/J/K line per-mesh evaluator (read-only).

## 이론적 근거 (≤30줄)

- **문제 정의**: VVV9N #1-2 가 R221/R222 에 helper `_evidence_compare_lines` (stellar.py §2579) +
  diag hook (mesher.py §3114-3129) 를 default OFF 로 안착. R223 에서 **default ON** 으로
  flip 해야 H/J/K 시뮬 데이터가 실제 bench / regression 로그에 기록됨 (read-only).
- **핵심 아이디어** (수식 / 단계):
  1. 1줄 flip: `_VVV9N_DIAG: bool = False` → `True`.
  2. **하드 가드**: gate 진입 전 `_n_sliver_pre >= 1 AND worst_mq_pre < 0.10` 만 통과
     (sliver 없는 mesh / 이미 우량 mesh 는 skip → wall 폭주 방지).
  3. **wall budget**: 3 라인 deepcopy + apply 는 ≤500ms / fid (helper 내장 wall_ms 측정,
     초과 시 다음 fid 부터 bailout — 단 read-only 라 mesh 영향 0).
  4. mesh array 변형 없음 (helper 내부 `copy.deepcopy` — 원본 immutable 보장).
- **단조 가드**: helper 자체가 dry_run only — `final_pts/final_tets` 미변경. 가드는 순수
  wall-time 제어 목적. exception 시 `log.warning(reason)` + skip.
- **레퍼런스**: Klingner 2008 §6 ablation; R197/R208/R220 (per-line monotone), R221 (#1), R222 (#2).
- **혁신성 평가**: novelty=2 (line-comparison ablation 데이터 수집 단계),
  rigor=2 (read-only + 이중 guard 단조 보장), impact=2 (다음 카드의 best-line
  선택자가 이 데이터에 의존). 합 = 6 ≥ 5.

## 변경

- 파일: `core/generator/native_tet/mesher.py` (단일)
- 함수: 메인 generator 루프, line ~3115 (VVV9N #2 hook 영역).
- 핵심 변경 (≤8줄 실질):
  1. `_VVV9N_DIAG: bool = False` → `True` (1줄).
  2. `if _VVV9N_DIAG:` → `if _VVV9N_DIAG and _n_sliver_pre >= 1 and _worst_pre < 0.10:`
     (where `_worst_pre = float(_pre_k1.min_q)` 또는 인접 `_qsnap` 1회 호출 — wall ~1ms).
  3. else 분기에서 `log.info("native_tet_vvv9n_skipped_guard", ...)` 1줄 (sliver=0 또는 worst≥0.10).
- 단조 가드: helper 가 deepcopy 사용 (mesh 영향 0) + 진입 가드 (sliver / worst).
- AVOID 마커: 기존 try/except 외곽 보존, 호출 인자 변경 금지, helper signature 변경 금지,
  `_VVV9N_DIAG` 식별자명 유지, `# VVV9N #2` 코멘트 유지.

## 검증 명령 (unit_tester 가 그대로 실행)

```bash
python3 -c "import ast; ast.parse(open('core/generator/native_tet/mesher.py').read()); print('SYNTAX_OK')"
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준 (validator)

- syntax: `ast.parse` PASS (필수, 첫 라인).
- 회귀 PASS (4 테스트, 0 fail).
- worst_mq: pre 0.208 ± 0.005 (read-only 라 ±0.005 자연 안전).
- bench: ≤ R222 (67.59s) + 100s = ≤ 167.59s (3 라인 deepcopy 비용 흡수).
- BL_OK 5/5 유지.
- 로그 `native_tet_vvv9n_diag` 또는 `native_tet_vvv9n_skipped_guard` 1회 이상 출현.

## 카드 시퀀스 위치

- VVV9N 시퀀스 (Klingner §6 line-ablation evidence): 3/5 카드.
  - #1 (R221) line-compare helper PASS.
  - #2 (R222) diag hook PASS (default OFF).
  - **#3 (R223, this card)** default ON + sliver/worst_mq guard.
  - #4 (예정) best-line selector → real apply (default OFF).
  - #5 (예정) selector default ON + per-fid 단조 가드.
- 다음 카드 후보 (PASS 후): `BETA2289_VVV9N4_BEST_LINE_APPLY_OFF` —
  `best_line` 결과로 H/J/K 중 1개 real apply (deepcopy 결과 채택), default OFF gate.
