# CARD BETA2283_VVV9K6_DIAG_ON (beta2283) — VVV9K #6: priority-queue diag gate flip ON

**target_engine**: tet
**모티프**: Klingner & Shewchuk 2008 §3.1 "Aggressive Tet Mesh Improvement" — priority-queue main-loop evidence collection (worst-first improvement attempt iteration)

## 이론적 근거

- **문제 정의**: VVV9K #1-5 (R213-R217) PASS 누적 — priority queue init/pop/improvement-attempt/main-loop helper + diag-hook (gate OFF). 본 카드는 evidence stream 활성화로 main-loop 의 (n_improved, n_iters_used, total_delta, wall_ms) 분포 수집.
- **핵심 아이디어**: 1줄 flip `_VVV9K_DIAG=False→True` (mesher.py:3082). main_loop 는 helper-only — `final_pts/final_tets` discard 유지. mesh state 불변. sliver-pre>=1 guard 이미 존재 (mesher.py:3083) — 모든 fid 호출 방지로 wall time 보호.
- **Wall time risk 분석**: improvement_attempt(1 vertex Newton) × max_iters=10 × time_budget_ms=100ms = O(1s) per-fid worst-case. sliver-pre>=1 gate 가 fid 절반 차단하면 5 fid × 1s = 5s overhead. R217 67.45s 대비 +5-10s 예상, +100s tolerance 안전.
- **레퍼런스**: Klingner 2008 §3.1 Algorithm 1; mesher.py:3081-3103 (R217 diag-hook); stellar.py `_priority_queue_main_loop` (R216).
- **혁신성 평가**: novelty 1 (gate flip), rigor 2 (sliver guard + discard 단조성), impact 2 (main-loop evidence 첫 수집). 합 5 — 통과.

## 변경

- 파일: core/generator/native_tet/mesher.py (단일 파일, 1-2줄)
- 함수: 라인 3082 `_VVV9K_DIAG: bool = False  # R217: default OFF` → `_VVV9K_DIAG: bool = True  # R218: gate flip ON`
- 핵심 변경:
  1. `_VVV9K_DIAG = True` (R218 flip — line 3082)
  2. (선택, 추가 안전) sliver-pre>=1 + `worst_mq < 0.20` 조건 추가 — 시간 budget 추가 보호
- 단조 가드: helper return tuple `_pts2/_tets2/_n_imp/_n_it/_delta` 모두 discard, `final_pts/final_tets` 절대 미할당 → mesh state 불변 → worst_mq ±0.005 자동 보장
- ≤80 줄 정책: 실제 변경 1-2 줄

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (회귀 0)
- worst_mq 0.208 ±0.005 (mesh state 불변)
- bench ≤ R217 67.45s + 100s tolerance (≤167.45s)
- log emit `native_tet_vvv9k_diag` per qualifying fid (n_improved, n_iters_used, total_delta, wall_ms)
- BL_OK 5/5 stable

## 카드 시퀀스 위치

VVV9K (Klingner priority-queue, total ~10 카드) 의 #6.
다음 후보: VVV9K #7 — diag stats aggregate (per-fid → roadmap accumulator) 또는 alt — improvement_attempt min_q 단조 조건 강화.

## AVOID 준수

- smooth_amips_analytic BSP 직후 호출 금지 (HHH1/III1/JJJ1)
- collapse_short_edges flip-후 호출 금지 (LLL1)
- flip_edges_54 default-ON 강행 금지 (VVV5)
- Steiner cavity insertion non-Delaunay 미구현 호출 금지 (VVV4/VVV4b)
