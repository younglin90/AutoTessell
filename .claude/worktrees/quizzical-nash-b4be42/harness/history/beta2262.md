# CARD BETA2262_VVV9H6_APPLY_DRYRUN_ON (beta2262) — VVV9H #6 apply dryrun gate flip True default

**target_engine**: tet
**모티프**: Klingner 2008 §4.1 edge contraction apply helper dryrun ON (evidence collection only, mesh discard)

## 이론적 근거 (≤30줄)

- **문제 정의**: R196 (BETA2261) wire 는 `_apply_klingner_edge_contract_topK` (top-K=10, triple monotone guard 내장) 를 sliver-pre>=1 gated 호출로 wire 했으나 gate OFF (default False). evidence (n_apply_attempted, n_apply_accepted, n_reverted, n_conflict, wall_ms) 수집 누락 — log emit 0회.
- **본 카드의 핵심 아이디어**:
  1. `_VVV9H_APPLY_DRYRUN: False → True` (mesher.py:2980, 1줄 flip).
  2. sliver-pre>=1 + len(_cands)>=1 guard 그대로 유지 (R196 wire skeleton).
  3. helper 호출 결과 (`_np, _nt, _st`) discard — `final_pts/final_tets` 재할당 없음 → mesh 무변경.
  4. log emit `native_tet_vvv9h4_dryrun` per-fid (mode="dry_run", n_apply_attempted=10, n_apply_accepted, n_reverted, n_conflict).
- **레퍼런스**: Klingner & Shewchuk 2008 §4.1 edge contraction; R196 BETA2261 wire (mesher.py:2979-2997). R185/R190 dryrun ON 패턴 동일 적용.
- **혁신성**: novelty 1 / rigor 2 / impact 1 = 4 (evidence-only diagnostic, 향후 apply 카드 정량 데이터 확보).

## 변경

- 파일: core/generator/native_tet/mesher.py (단일)
- 함수: `mesh()` line ~2980
- 핵심 변경 (1줄):
  1. `_VVV9H_APPLY_DRYRUN: bool = False` → `True` (주석 갱신: R197 flip ON).
- 단조 가드: helper 결과 discard 구조 그대로 (mesh unchanged), sliver-pre>=1 + len(_cands)>=1 guard 유지.

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (31/31)
- worst_mq 0.2069 ±0.005
- bench ≤ R196 (61.78s) + 50s tolerance = 111.78s
- mesh 무변경 (helper 결과 discard, final_pts/final_tets 재할당 없음)
- log `native_tet_vvv9h4_dryrun` 1+회 emit (sliver-pre>=1 fid)
- BL_OK 5/5

## 카드 시퀀스 위치

- VVV9H sequence #6 of ~7 (Klingner edge contract dryrun → 다음 단계 default ON).
- 다음 카드 후보: BETA2263_VVV9H7_APPLY_DEFAULT_ON (evidence 분석 후 mesh 적용 활성, accepted ≥1 + worst_q monotone 보장 시).

## AVOID 마커 준수

- smooth_amips_analytic/multistage BSP 직후 호출 금지 (HHH1/JJJ1) — 본 카드 무관.
- collapse_short_edges KKK1 flip 후 금지 (LLL1) — 본 카드는 helper discard, 영향 없음.
- flip_edges_54 default-ON post-VVV3b without per-flip min_q strict guard 금지 (VVV5) — 본 카드 무관.
- Steiner cavity insertion non-Delaunay 카드 금지 (VVV4/VVV4b) — 본 카드 무관.
- 단순 매개변수 sweep 3회 연속 금지 — 본 카드는 dryrun gate flip evidence 수집 (R185/R190 패턴 적용).
