# CARD BETA2255_VVV9F5_DRYRUN_WIRE (beta2255) — VVV9F #4 dry-run wire (perturb_topK + select_best, mesh discard, log-only)

**target_engine**: tet
**모티프**: Cheng-Dey-Edelsbrunner-Facello-Teng 1999 §4 Algo 4.1 (step1 sample + step2 argmax) — diagnostic dry-run wire (R184 _VVV9D_DRYRUN 패턴 그대로). Mesh state untouched; emits per-fid evidence log only.

## 이론적 근거 (≤30줄)

- **문제 정의**: VVV9F #2/#3 (R188/R189) 가 두 helper (`_perturb_weights_topK`, `_select_best_weight_assignment`) 를 모듈 단위 skeleton 으로 추가했지만 **caller 가 없어** 실제 입력 분포 (l_min spread, alpha=0.3 효과, K=8 worst→best Δ) 에 대한 evidence 가 0건. R190 은 `mesher.py` VVV12 의 sliver_split log 직후에 dry-run 호출을 wire 하여 mesh 미변경 + 로그 한 줄로 (n_samples, alpha, best_idx, best_min_q, wall_ms) 수집.
- **핵심 단계**: 기존 `_VVV9D_DRYRUN` block (line 2908-2930) 직후 동일 try-except 패턴으로:
  1. `W = _perturb_weights_topK(final_pts, final_tets, n_samples=8, alpha=0.3, seed=0)` — (8,N) matrix.
  2. `(best_idx, best_min_q) = _select_best_weight_assignment(final_pts, final_tets, W, alpha=0.3)` — argmax row.
  3. 결과 discard, log key `native_tet_vvv9f_dryrun` 에 (n_samples, alpha, best_idx, best_min_q, wall_ms, mode="dry_run") emit.
- **단조 보장**: helper 두 개 모두 pure (mesh arrays read-only, R188/R189 docstring assert). Gate `_VVV9F_EXUDATION_DRYRUN=False` default OFF — 시퀀스 다음 카드 (R191) 에서 True flip.
- **레퍼런스**: Cheng-Dey 1999 FOCS §4 Algo 4.1; `core/generator/native_tet/stellar.py:1206,1273`; R184 (`mesher.py:2908-2930`) wire 패턴.
- **혁신성**: novelty=1 (기존 wire 패턴), rigor=2 (default OFF + helper pure), impact=2 (R191 ON 직전 evidence 채널 확보). 합=5 — 시퀀스 위치상 OK.

## 변경

- 파일: `core/generator/native_tet/mesher.py` (단일 파일, ≤25줄)
- 위치: 함수 `_native_tet_pipeline` 내 line ~2930 직후 (기존 `_VVV9D_DRYRUN` block 종료와 `except Exception as exc:` line 2931 사이; VVV12 try block 안)
- 핵심 변경:
  1. `_VVV9F_EXUDATION_DRYRUN: bool = False  # R190/VVV9F #4 default OFF — gate ON in R191`
  2. `if _VVV9F_EXUDATION_DRYRUN:` → try-except (R184 패턴)
  3. `from core.generator.native_tet.stellar import _perturb_weights_topK as _pwk_dr, _select_best_weight_assignment as _sbw_dr` (lazy, noqa: PLC0415)
  4. `_t0 = time.perf_counter()`
  5. `_W = _pwk_dr(final_pts, final_tets, n_samples=8, alpha=0.3, seed=0)`
  6. `_bidx, _bmq = _sbw_dr(final_pts, final_tets, _W, alpha=0.3)`
  7. `_wall_ms = int((time.perf_counter() - _t0) * 1000)`
  8. `log.info("native_tet_vvv9f_dryrun", n_samples=8, alpha=0.3, best_idx=int(_bidx), best_min_q=float(_bmq), wall_ms=int(_wall_ms), mode="dry_run")`
  9. `except Exception as exc:  # noqa: BLE001` → `log.warning("native_tet_vvv9f_skipped", reason=str(exc)[:120])`
- 단조 가드: gate=False → 전체 block skip → mesh arrays unchanged → 회귀 0. helper pure 보장 (R188/R189 commit 시 assert read-only). final_pts / final_tets 미변형 (return 값 discard).

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (4 file 좁은 환경, env-fail 회피)
- bench 시간 ≤ 580 + 15% (gate OFF → no-op)
- tet metric: worst_mq 0.2069 ± 0.005, mean_q ± 0.005
- mesh arrays bit-identical (gate OFF, return discard)
- BL_OK 20/20 동등

## 카드 시퀀스 위치

VVV9F (Cheng-Dey 1999 sliver exudation) sequence 4/6:
- #1 BETA2252_VVV9F2_DRYRUN_REVERT (R187, PASS — VVV9D revert)
- #2 BETA2253_VVV9F3_PERTURB_TOPK_HELPER (R188, PASS — helper #1)
- #3 BETA2254_VVV9F4_SELECT_BEST (R189, PASS — helper #2)
- **#4 BETA2255_VVV9F5_DRYRUN_WIRE (R190, this card, gate OFF)**
- #5 BETA2256_VVV9F6_DRYRUN_ON (R191, gate=True, evidence emit, mesh still unchanged)
- #6 BETA2257_VVV9F7_APPLY (R192, mesh apply with worst-mq revert guard)

AVOID 준수: `_VVV9D_DRYRUN` (R187 에서 False revert) 미건드림. 신규 gate `_VVV9F_EXUDATION_DRYRUN`. AMIPS 직후 호출 / collapse_short_edges 패턴 미해당.

다음 카드 (R191): BETA2256_VVV9F6_DRYRUN_ON — gate flip True, fid 별 evidence 1줄 수집 (mesh still discard).
