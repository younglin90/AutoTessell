# CARD BETA2284_VVV9K7_REAL_APPLY_ENV (beta2284) — VVV9K #7 env-only real-apply gate

**target_engine**: tet
**모티프**: R197 (VVV9H6) / R212 (VVV9J apply env-gate) — env-only opt-in real-apply, default OFF, mesh unchanged at bench

## 이론적 근거 (≤30줄)

- **문제 정의**: R218 (BETA2283_VVV9K6_DIAG_ON) 에서 priority-queue main-loop (Klingner 2008 §3 + Stellar) 가 ON 되어 evidence (n_improved, n_iters_used, total_delta, wall_ms) 가 per-fid 수집 중. 그러나 helper 결과 `(_pts2, _tets2)` 는 즉시 discard. real-apply 단계로 이행하기 전 **승인된 R197 패턴 (env-only opt-in)** 을 적용하여 dev 측정·empirical evidence 수집을 default-OFF 로 가능케 함.
- **핵심 아이디어**:
  1. 신규 sentinel `_VVV9K_APPLY_REAL = bool(os.environ.get("AUTO_TESSELL_VVV9K_APPLY", "0") == "1")` (default False).
  2. `_VVV9K_DIAG` try-block 안 helper 호출 직후, `_VVV9K_APPLY_REAL` True 시 `_delta ≥ 0` + `_n_imp ≥ 1` 가드 통과하면 `final_pts, final_tets = _pts2, _tets2` reassign.
  3. log emit `native_tet_vvv9k7_real_apply` (n_improved, total_delta, wall_ms, mode=apply).
  4. **합격 측정은 env unset (=default 0) 기준** — bench / regression test 모두 mesh 무변경.
- **단조 보장**: env unset 시 코드 경로 unchanged (R197/R212 동일 패턴, 4회 PASS 누적).
- **레퍼런스**: R197 (BETA2262_VVV9H6_APPLY_DRYRUN_ON, mesher.py:2982/2998), R212 (VVV9J apply env-gate, mesher.py:3054), Klingner 2008 §3 priority queue.
- **혁신성**: novelty 1 (기존 패턴 재적용), rigor 3 (env-default-OFF + 단조 가드), impact 2 (real-apply path 준비). 합 = 6 ≥ 5 ✓.

## 변경

- 파일: core/generator/native_tet/mesher.py (단일 파일)
- 함수: `_main_meshing_loop` 내 `_VVV9K_DIAG` try-block (line ~3082-3103)
- 핵심 변경 (≤25줄):
  1. line 3082 직후: 신규 `_VVV9K_APPLY_REAL: bool = bool(os.environ.get("AUTO_TESSELL_VVV9K_APPLY", "0") == "1")`.
  2. line 3100 (results discard 주석 직전): `if _VVV9K_APPLY_REAL and _delta >= 0.0 and int(_n_imp) >= 1:` → `final_pts, final_tets = _pts2, _tets2` + log.info("native_tet_vvv9k7_real_apply", n_improved, total_delta, wall_ms, mode="apply").
  3. 주석 update: "results discard — final_pts/final_tets unchanged unless AUTO_TESSELL_VVV9K_APPLY=1".
- 단조 가드: env unset → no behavior change. env set → `_delta ≥ 0` + `_n_imp ≥ 1` 가드 (priority-queue 자체 monotone 가드 신뢰).

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (env unset)
- worst_mq 0.208 ±0.005 (default OFF, R218 baseline 동등)
- bench_time ≤ R218 + 5% (67.59s → ≤71s)
- BL_OK 5/5 stable
- 카드 변경 ≤ 80줄

## 카드 시퀀스 위치

VVV9K (Klingner 2008 priority-queue main-loop) 시퀀스 #7 of ~9. 다음 카드 후보 (R220): VVV9K #8 — `AUTO_TESSELL_VVV9K_APPLY=1` opt-in dev-bench 실측 + 단조 가드 강화 (per-iter post.min ≥ pre.min) 카드.

## AVOID 마커 준수

- flip_edges_54 default-ON 금지 (VVV5 reject) — 본 카드 무관 ✓.
- Steiner cavity insertion 금지 (VVV4 timeout) — 본 카드 priority-queue main-loop 만, 무관 ✓.
- 매개변수 sweep 카드 금지 — 본 카드 알고리즘 차원 env gate 추가 ✓.
