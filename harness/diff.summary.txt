CARD HEX-WALLFIT-BACKTRACK (beta1) — bisection backtrack replaces all-or-nothing revert

file: core/generator/native_hex/mesher.py
functions: _wall_fit_snap
lines_added: 23
lines_removed: 5
- stats dict gained `n_snapped_partial` counter (init 0).
- vol-reject `else` branch: full projection that fails `_cell_ok` is no
  longer reverted outright. Bisects (<=12 iters) orig->p0 for the largest
  fraction `lo` that still passes the same `_cell_ok` guard on every
  incident cell; accepted only if `lo>0` AND the strict-decrease check
  (`d_partial < d0 - 1e-15`, same criterion as the full-projection path)
  holds. Otherwise reverts to `orig` and counts `n_reject_vol` as before.
- `_cell_ok` itself untouched (guard not weakened, only a better solution
  found within it).

file: tests/test_native_hex_solid_volume.py
functions: test_native_hex_curved_wall_fidelity
lines_added: 13
lines_removed: 17
- removed `pytest.mark.xfail(strict=True)` on the "fine" param; now a
  plain `["standard", "fine"]` parametrize list.
- docstring STATUS section appended with this card's measured results.

RESULT (measured, timeout 300 python3 -m pytest tests/test_native_hex_solid_volume.py -q):
- 6 passed in 56.71s (all, including fine — previously xfail).
- fine (cylinder N=2000): n_target=350 n_snapped=320 n_snapped_partial=30
  n_reject_vol=0. wall_dev_max=0.008015 (was ~0.035, gate <=0.02) mean=0.001380.
- standard: n_reject_vol=0 (this code path never triggers, no-op by
  construction). wall_dev_max=0.003241 — bit-identical to pre-change.
- negative_volumes=0 in every measured case (cube 4-invariant + cylinder
  standard/fine) — safety invariant intact.
