BETA2827 BLOCK — card stale + regresses. CYL baseline bskew=280 (card premise 4159; block skipped as card itself notes); scale-relative 3-2 flip (n_flip 11) deterministically REGRESSES CYL boundary skew 280 -> 1.26e4 (card expected 179.7). Cause: flip lowers boundary skew at block stage (bskew_post=44.94) but downstream BETA2826 locked-smooth then re-worsens the flipped mesh to bskew 1.26e4 (internal skew unchanged 3.35->3.28); card guard only measures at block stage so it cannot catch this. Also card literal guard `sv6(work).min()<=0` (absolute) reverts the whole removal on the N=500 cube's pre-existing inverted tet (min_sv6=-0.0069, not a flat sliver), regressing cube skew 3.8->5.89e28 — needs relative `n_inv_post>n_inv_pre`. Reverted mesher.py to clean (cube N=2000 skew 1.81 OK, N=500 skew 3.8 OK). Re-plan: re-baseline against current repo (CYL final bskew=280 not 4159) and move the bskew/inversion guard to AFTER BETA2826 (post-write) or gate the flip acceptance on the post-smooth boundary skew, else the flip is boundary-skew-negative net.
