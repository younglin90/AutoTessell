BETA2823: lock itself is CORRECT (moved 133->0, per-call off-plane stays 0.0, div-volume 0.750->1.0000 exact, skew 107.9->10.05, axis-aligned 14.9%->79.0%) but card criterion 4 FAILS — locking starves the smoothing half of smooth_then_drop_slivers (validate.py:128 `affected &= ~locked_mask`), so n_drop/call goes 4->~50 and the DROP half punches voids: bnd faces 446->574, boundary area 6.129->7.903 (1.32x), test_native_tet_solid_volume FAILS. Card's "revert 0회 = lock 정확성 증명" signal is confounded: the area_coverage guard is structurally blind to interior deletion (same blindness documented at test_native_tet_solid_volume.py:26-29), so it fired 0x while area regressed 29%. Next card must make this call-site void-free (drop half must not delete tets) together with the lock.
