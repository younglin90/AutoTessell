# Improvement plan — native-all-production-gate-067

## Goal

native-all-production-gate-067

## Scope and invariants

-

## Planned card

- Mechanism:
- Default state:
- Expected benefit:
- Failure/rollback condition:

## Quality and authority gates

-

## Verification ladder

- L0:
- L1:
- L2:
- L3:

## Evidence to preserve

-


## Planner transport and card 067-A

Sole planner agent 019fcdc4-c17d-7870-a5f1-9ba6a42d1e61 (Helmholtz), requested gpt-5.6-terra, high reasoning, priority, fast OFF lifecycle policy. The API exposes no fast field; that limitation is recorded honestly. It completed normally after the long wait and did not implement or test.

The card is ActualWriterArtifactContract. Add a reusable C++23 WriterArtifactV1 contract and canonical binary serializer/rereader. Native Tet and surface wall-edge writers must directly issue artifact hash/size/schema, writer UIDs, source/parent IDs, CAD face/edge or certified STL IDs, feature/patch/physical-group/component/provenance, strict topology zeros, family quality witness, BL=0 identity or BL>=1 exact roles/layers/positive measure/collision visibility.

AQTE validates but never computes, repairs, or mints any of those fields. Independent reread must reconstruct matching artifact/source/UID/lineage/quality digests before publish. Quality order is topology -> positive measure/orientation -> authority/lineage -> BL -> signed non-orthogonality/skewness -> family aspect -> count. C++ owns the hot path; Python only transports.

L0: missing UID/quality, tamper, callback exception, and rollback. L1: actual Tet/surface cube and curved patch BL=0/1 with staged bytes and reread parity. L2: sphere/NACA/ridge/narrow-gap/T-junction/complex CAD/STL and BL=0/1/3/8. L3: separate-process reread, repeat digest, package provenance and tamper.

The main session has confirmed the 066 callback and the current writer gaps. Implementation must not alter protected Poly.

## Filled implementation gate

- Surface BL=0 identity, surface BL=1 wall-edge strip, and Tet BL=1 authoritative artifact bridge are the implemented cards for this round.
- Each actual writer must provide schema, bytes, byte size, writer hash, UIDs, lineage rows, topology zeros, quality witness, and BL witness before AQTE can publish.
- The implementation is C++23; Python is used only to invoke the compiled writer and transaction boundary in tests.
- The round is partial until a real Tet BL=0 volume artifact and the independent complex-source/release corpus are available.
