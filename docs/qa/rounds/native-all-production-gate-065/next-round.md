# Next round - native-all-production-gate-065

Next priority: bind the executor to actual writer artifacts, beginning with Native Tet and the surface wall-edge BL route, then carry the same callback contract to Native Hex, Native Tri, Strict Quad and TRI+QUAD without changing protected Poly.

Required evidence per product:

- BL=0 and BL>=1 actual output, with no-op/positive-layer work counters and atomic rollback.
- Source authority and writer-issued UID/feature/patch/physical-group/component/provenance parity.
- Independent disk reread and repeat hash in a separate process.
- Family quality distributions and worst IDs for signed non-orthogonality, skewness, aspect ratio, and positive measure before count tuning.
- Cube, sphere, NACA/ridge, narrow-gap/T-junction, complex CAD/B-Rep and STL corpus; safe refusal is acceptable, partial output is not.
- Every user-controlled input either reaches a named writer sink or produces a deterministic unsupported refusal; no hard-coded replacement.

Known literature blockers remain `10.1002/nme.7644` and `10.1016/j.compfluid.2026.107032`; do not derive production thresholds from them until full text is available.
