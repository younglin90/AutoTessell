# Tet Pre-relocation Strict Evidence L1

Three deterministic sphere runs freeze the pre-SSS-pass0 strict-invalid state:
`pre_sss_pass0_relocate` has `350` inverted tets and `116` same-side internal
faces.  This is evidence only; no repair or threshold change.
