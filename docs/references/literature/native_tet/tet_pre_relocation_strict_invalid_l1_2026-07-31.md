# Tet Pre-relocation Strict-invalid L1

Test-only observer now records existing inversion and same-side audits at every
checkpoint, deterministically across three sphere runs.  This card is evidence
instrumentation only; no runtime behavior changes.  Exact earliest-stage values
remain to be promoted into explicit assertions by next localization card.
