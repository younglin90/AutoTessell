# CDT Notes - 2026-07-22

## CDT1 Recovery Diagnostic

Paper basis:

- TetGen/Shewchuk-style boundary recovery should split constrained segments only
  after checking encroachment and valid insertion cavity.
- TetWild/fTetWild-style dirty input handling should avoid broad rescue
  heuristics and prefer rollback-safe local operations with explicit blockers.

Implemented first low-risk step:

- `diagnose_cdt_recovery_blockers(...)` in `cdt_recovery.py`.
- No mesh mutation.
- Classifies missing surface-edge midpoint insertion blockers:
  invalid edge, duplicate midpoint, no Bowyer-Watson cavity, oversized cavity,
  protected-edge encroachment, empty cavity boundary, ready for insertion.

Why this matters:

- Current CDT recovery can revert without saying whether failure came from
  topology, duplicate Steiner point, protected constraint, or cavity size.
- Next CDT/native operation can target the dominant blocker instead of adding
  broad dirty-STL heuristics.

Next blocker:

- Wire diagnostics into hard-case matrix logging before adding stronger triangle
  insertion or shell-cavity recovery.

## CDT-E2E Diagnostic - hard focus

Command:

- `python3 scripts/verify_native_tet_replacement_matrix.py --run-root /tmp/autotessell_tet_cdt_e2e_v3 --source tests/stl/03_hard_bracket.stl --source tests/benchmarks/pipe.step --source tests/stl/04_extreme_gear.stl --max-cells 2000 --bl-layers 3 --timeout 600 --cdt-cycles 1 --cdt-outer-iter 1 --cdt-points-budget 40`
- `python3 scripts/verify_native_tet_replacement_matrix.py --run-root /tmp/autotessell_tet_cdt_e2e_mixed_v1 --source /tmp/autotessell_tet_cdt_inputs/mixed_watertight_and_open__scale_aniso.stl --max-cells 2000 --bl-layers 3 --timeout 600 --cdt-cycles 1 --cdt-outer-iter 1 --cdt-points-budget 40`

Result:

- 4/4 PASS.
- 03_hard_bracket: missing 604, duplicate 15, no_cavity 70, ready 115.
- pipe.step: missing 1388, duplicate 2, no_cavity 99, ready 99.
- 04_extreme_gear: missing 1446, duplicate 10, no_cavity 40, ready 150.
- mixed_watertight_and_open scale_aniso: missing 244, duplicate 0, no_cavity 59, protected_encroachment 1, ready 140.

Dominant blockers:

- Largest surface conformity gap is still unrecovered missing constraints, not
  invalid edges.
- Direct midpoint insertion has enough "ready" cases to justify a bounded
  triangle/shell insertion card, but `no_cavity` remains large enough that pure
  Bowyer-Watson midpoint insertion cannot close the gap alone.
- Oversized cavity is not dominant in these four cases.

Next coding card:

- `CDT2-SHELL`: for missing edges classified `ready`, insert a bounded shell
  cavity segment/triangle recovery pass with strict rollback and CDT ratio gate.
- `CDT3-NOCAVITY`: for `no_cavity`, add local star/shell cavity expansion before
  Steiner insertion; keep max cavity cap and protected-edge encroachment guard.
