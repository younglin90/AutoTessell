# I-series — BETA2619+ 잔여 격차 마무리 (2026-04-30)

## Context

H-series (BETA2610-2618) 완료. 38+ 카드 / 592 tests PASS. 잔여 multi-week 작업
(RL, MPI, .cu kernel, Web SaaS) 외에도 atomic edit 가능 추가 격차:

1. CFD 출력 포맷 — Tecplot .plt / NASA Plot3D 부재.
2. ML predictor architecture — 단순 MLP, 정확도 향상 여지.
3. Strategist ML hint — geometry 분류만, 학습 기반 추천 부재.
4. bench profiling breakdown — total time 만, stage별 부재.
5. 통합 export round-trip 테스트 — 포맷별 분산.
6. RELEASE_NOTES — beta2581-2618 누적 changelog 부재.

## Goal

**6 추가 격차 카드 + 회귀 + plan close = 8 cards (I1-I8)**.

## Scope

- `core/utils/` — Tecplot/Plot3D writer
- `core/generator/native_ai/` — predictor architecture v3
- `core/strategist/` — ML hint
- `tests/` — round-trip + bench profiling
- `docs/` — RELEASE_NOTES

## Metric

- 카드 완료 수 (8).
- 회귀 PASS (≥ 592).
- 신규 출력 포맷 round-trip 검증.

## Direction

higher is better.

## Verify

```bash
timeout 240 python3 -m pytest \
  tests/test_native_tet_amips.py tests/test_native_hex.py \
  tests/test_native_poly.py tests/test_native_ai.py \
  tests/test_cvt3d_aniso_cvt.py tests/test_self_intersect.py \
  tests/test_native_repair.py tests/test_native_hex_snap.py \
  tests/test_qt_app.py tests/test_mixed_pyramid.py \
  tests/test_cli_integration.py tests/test_strategist.py -q -k "not slow"
```

## 카드 (I1-I8)

| ID | 카드 | 시간 | 변경 파일 | 효과 |
|----|------|------|----------|------|
| **I1** | Tecplot .plt ASCII writer | 1h | `core/utils/tecplot_writer.py` (NEW ~120 줄) | Tecplot 360 직접 import |
| **I2** | NASA Plot3D .x/.q writer | 1h | `core/utils/plot3d_writer.py` (NEW ~100 줄) | NASA legacy CFD 솔버 호환 |
| **I3** | ML predictor v3 (residual MLP) | 1h | `core/generator/native_ai/train_predictor.py` (~30 줄) | val_loss 0.005 → ?  |
| **I4** | bench stage profiling | 30min | `tests/stl/bench_difficulty_tiers.py` (~40 줄) | total/repair/gen/bl 분리 |
| **I5** | export 통합 round-trip 테스트 | 30min | `tests/test_export_formats.py` (NEW ~100 줄) | 6 포맷 회귀 |
| **I6** | Strategist ML hint integration | 1h | `core/strategist/tier_selector.py` (~30 줄) | trained model 로 tier 추천 |
| **I7** | RELEASE_NOTES.md | 30min | `RELEASE_NOTES.md` (NEW) | beta2581-2618 changelog |
| **I8** | 최종 회귀 + plan close | 30min | (commit only) | ≥ 592 tests PASS |

총: ~5 시간 작업.

## 합격 기준

각 카드 끝마다 unit regression PASS. 연속 3 fail 시 plan 중단.

## 의존성

```
I1, I2 (independent — output format)
I3 (independent — ML)
I4 (independent — bench)
I5 → after I1, I2 (round-trip 테스트)
I6 (independent — strategist)
I7 (independent — docs)
I8 (last — final regression)
```
