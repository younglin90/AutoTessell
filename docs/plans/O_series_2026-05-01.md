# O-series — BETA2660+ (2026-05-01)

## Context

N-series (BETA2653-2659) 후 잔여 atomic 격차:
1. STL/OBJ/PLY surface writer (현재 reader 만 있음).
2. Native quick-mesh CLI (single-line script).
3. Mesh metrics JSON dump utility.
4. CLI command tree 자동 documentation.
5. ML model loader 안정성 (load 실패 시 명확한 에러).
6. AnalyzerReport 의 추가 필드 노출 (CLI).
7. O-series tests.
8. Final regression.

## Goal/Scope/Metric/Verify

- Goal: 7 카드 + 회귀 PASS (≥621).
- Scope: `core/utils/`, `cli/`, `tests/`, `scripts/`.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **O1** | STL ASCII writer | 30min | `core/utils/stl_writer.py` (NEW) |
| **O2** | OBJ + PLY writer | 45min | `core/utils/obj_ply_writer.py` (NEW) |
| **O3** | Native quick-mesh CLI | 30min | `scripts/quick_mesh.py` (NEW) |
| **O4** | Mesh metrics JSON dump | 30min | `scripts/dump_mesh_metrics.py` (NEW) |
| **O5** | CLI command tree dump | 30min | `scripts/dump_cli_tree.py` (NEW) |
| **O6** | ML loader robustness | 30min | `core/generator/native_ai/ml_tet_smoothing.py` (~15 줄) |
| **O7** | O-series tests | 30min | `tests/test_o_series.py` (NEW) |
| **O8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
