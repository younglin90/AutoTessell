# S-series — BETA2688+ (2026-05-01)

## Context

R-series (BETA2681-2687) 후 잔여 atomic 격차:
1. Volume mesh sub-region extract (bbox crop).
2. Mesh boolean union helper (surface).
3. CLI doctor 의 environment summary 부가 정보.
4. ML model checksum verify utility.
5. Cell quality 변화 추적 (before/after mesh op).
6. Tier registry export (programmatic API).
7. S-series tests.
8. Final regression.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **S1** | Volume mesh bbox crop | 45min | `core/utils/mesh_crop.py` (NEW) |
| **S2** | ML model checksum verify | 30min | `scripts/verify_model_checksum.py` (NEW) |
| **S3** | Cell quality delta tracker | 45min | `core/analyzer/quality_delta.py` (NEW) |
| **S4** | Tier registry programmatic API | 30min | `core/strategist/tier_selector.py` (~30 줄) |
| **S5** | Doctor environment summary | 30min | `cli/main.py` (~40 줄) |
| **S6** | Smoke test runner | 30min | `scripts/run_all_smoke.py` (NEW) |
| **S7** | S-series tests | 30min | `tests/test_s_series.py` (NEW) |
| **S8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
