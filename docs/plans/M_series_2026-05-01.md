# M-series — BETA2647+ (2026-05-01)

## Context

L-series (BETA2640-2646) 후 잔여 atomic 격차:
1. VTU binary mode (현재 ASCII만 — 큰 mesh 느림).
2. Tier auto-fallback 체인 logging.
3. Predictor inference batching API.
4. Volume mesh statistics module (cell quality 분포).
5. STEP/IGES output via cadquery (CAD output).
6. ML training data scale 시도 (50 mesh × 500 sample).
7. Cell quality colormap 도구.
8. 최종 회귀.

## Goal/Scope/Metric/Verify

- Goal: 7 카드 + 회귀 PASS (≥612).
- Scope: `core/utils/`, `cli/`, `tests/`, `scripts/`.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **M1** | VTU binary append mode | 1h | `core/utils/vtk_writer.py` (~70 줄) |
| **M2** | Tier fallback chain logger | 30min | `core/strategist/tier_selector.py` (~20 줄) |
| **M3** | Predictor batching API | 45min | `core/generator/native_ai/inference_batch.py` (NEW) |
| **M4** | Volume mesh statistics | 45min | `core/analyzer/volume_stats.py` (NEW) |
| **M5** | Larger ML training (3rd run) | 30min | (자동, scripts 호출만) |
| **M6** | Cell quality colormap export | 30min | `scripts/export_quality_vtk.py` (NEW) |
| **M7** | M-series 통합 회귀 | 30min | `tests/test_m_series.py` (NEW) |
| **M8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
