# H-series 카드 — BETA2610+ 추가 격차 메우기 (2026-04-30)

## Context

G-series (BETA2602-2609) 완료 후, 잔여 격차 분석:

| 격차 영역 | 상태 |
|----------|------|
| 출력 포맷 | StarCCM+ ASCII/binary/CCMIO + CGNS — but Fluent/VTK/Tecplot 미지원 |
| Mixed-element pipeline 통합 | mixed_pyramid 모듈 NEW but native_hex 와 wire 안 됨 |
| y+ adaptive BL | 부분 구현 — explicit 자동화 필요 |
| ML feature engineering | 12-dim only — curvature tensor 추가 가능 |
| Geometry-driven mesh_type 추천 | 미구현 (사용자 수동 선택) |
| 통합 시나리오 테스트 | CLI flag 조합 회귀 부재 |
| bench result export | print only — TSV/JSON 자동 export 부재 |
| Real Thingi10K | thingi10k 라이브러리 통합 안 됨 |
| Algorithm 문서화 | usage 만, 알고리즘 spec 부재 |

## Goal

**8 추가 격차 카드 (H1-H8) + 회귀 (H9) + 계획 종료 (H10)**.

## Scope

- `core/utils/` — Fluent / VTK 출력 포맷
- `core/layers/` — mixed-element 통합 + y+ 적응
- `core/generator/native_ai/` — feature 강화
- `core/strategist/` — geometry-driven 추천
- `tests/` — 통합 회귀
- `scripts/` — 자동화
- `docs/algorithms/` — 알고리즘 spec

## Metric

- 카드 완료 수 (10).
- 회귀 PASS (≥428).
- 신규 출력 포맷 round-trip 검증.

## Direction

higher is better.

## Verify

```bash
# 단위 회귀
timeout 240 python3 -m pytest \
  tests/test_native_tet_amips.py tests/test_native_hex.py \
  tests/test_native_poly.py tests/test_native_ai.py \
  tests/test_cvt3d_aniso_cvt.py tests/test_self_intersect.py \
  tests/test_native_repair.py tests/test_native_hex_snap.py \
  tests/test_qt_app.py tests/test_mixed_pyramid.py -q -k "not slow"
```

## 카드 (H1-H10)

| ID | 카드 | 시간 | 변경 파일 | 효과 |
|----|------|------|----------|------|
| **H1** | Fluent .msh writer | 1h | `core/utils/fluent_writer.py` (NEW ~150 줄) | ANSYS Fluent 직접 import |
| **H2** | VTK .vtu polyhedral writer | 30min | `core/utils/vtk_writer.py` (NEW ~100 줄) | ParaView 호환 |
| **H3** | mixed-element pipeline wire | 1h | `core/layers/native_bl.py`+`core/generator/native_hex/mesher.py` (~30 줄) | tet+hex 인터페이스 활용 |
| **H4** | y+ adaptive first thickness | 30min | `core/layers/native_bl.py` (~20 줄) | wall law-of-the-wall fit |
| **H5** | ML curvature feature 추가 (12 → 14-dim) | 1h | `core/generator/native_ai/training_data.py` (~30 줄) | mean+gauss curvature 사용 |
| **H6** | geometry-driven mesh_type 추천 | 1h | `core/strategist/strategy.py` (~40 줄) | 자동 hex/tet/poly 선택 |
| **H7** | bench result TSV/JSON auto-export | 30min | `tests/stl/bench_difficulty_tiers.py` (~20 줄) | 결과 자동 저장 |
| **H8** | 통합 CLI 시나리오 테스트 | 30min | `tests/test_cli_integration.py` (NEW ~80 줄) | flag 조합 회귀 |
| **H9** | 알고리즘 spec 문서 | 30min | `docs/algorithms/index.md` (NEW) | 알고리즘 reference |
| **H10** | 최종 회귀 + plan close | 30min | (commit only) | 428+ tests PASS |

총: ~7 시간 작업.

## 합격 기준

각 카드 끝마다 unit regression PASS (≥428). 연속 3 fail 시 plan 중단.

## 의존성

```
H1, H2, H3, H4, H5, H6 (independent — 병렬 가능)
H7 (independent)
H8 → H10 (last)
```

각 카드 atomic edit, commit.
