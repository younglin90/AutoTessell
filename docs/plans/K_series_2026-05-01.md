# K-series — BETA2633+ 추가 (2026-05-01)

## Context

J-series (BETA2626-2632) 후 잔여 atomic 격차:
1. Patran/Nastran FEM format (구조 해석 호환).
2. Surface diagnostic 도구 (face normal consistency).
3. ML training data augmentation (rotation invariance).
4. Progress callback API (long ops).
5. Quality histogram dump utility.
6. Plugin discovery (custom tier 등록).
7. CLAUDE.md v0.5 update.
8. 최종 회귀.

## Goal/Scope/Metric/Verify

- Goal: 8 카드 + 회귀 PASS (≥ 603).
- Scope: `core/utils/`, `core/generator/native_ai/`, `cli/`, `tests/`, `docs/`, `CLAUDE.md`.
- Metric: 카드 완료 수 + 회귀 PASS.
- Verify: 12 test suite pytest + 신규 export round-trip.
- Direction: higher.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **K1** | Nastran .bdf writer | 1h | `core/utils/nastran_writer.py` (NEW) |
| **K2** | Surface diag (normal consistency) | 30min | `core/analyzer/surface_diag.py` (NEW) |
| **K3** | ML training augmentation | 45min | `core/generator/native_ai/training_data.py` (~30 줄) |
| **K4** | Progress callback API | 1h | `core/utils/progress.py` (NEW) |
| **K5** | Quality histogram dump | 30min | `scripts/dump_quality_histogram.py` (NEW) |
| **K6** | Tier plugin discovery | 45min | `core/generator/plugin_loader.py` (NEW) |
| **K7** | CLAUDE.md v0.5 update | 30min | `CLAUDE.md` (~30 줄) |
| **K8** | 최종 회귀 | 30min | (commit only) |

총 ~5 시간.

## 합격 기준

각 카드 commit + 회귀 PASS. 연속 3 fail 시 plan 중단.
