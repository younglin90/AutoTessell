# J-series — BETA2626+ 추가 격차 (2026-04-30)

## Context

I-series (BETA2619-2625) 완료 후 잔여 atomic 격차:

1. AVS UCD format (Salome/FieldView 호환).
2. Gambit .neu (legacy Fluent).
3. pre-flight STL validator script (mesh 시작 전 sanity).
4. CLI config 파일 지원 (`--config FILE`).
5. GUI 키보드 shortcut.
6. 중앙 error catalog (사용자 친화 메시지).
7. bench diff script (두 run 비교).
8. 최종 회귀.

## Goal

8 atomic cards — output 다양화 + UX 향상 + 회귀.

## Scope

`core/utils/`, `cli/`, `desktop/qt_app/`, `scripts/`, `tests/`, `docs/`.

## Metric / Verify

- 카드 완료 수 (8).
- 회귀 PASS (≥ 601 tests, plan target ≥ 605).

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **J1** | AVS UCD writer | 45min | `core/utils/avs_ucd_writer.py` (NEW) |
| **J2** | Gambit .neu writer | 45min | `core/utils/gambit_neu_writer.py` (NEW) |
| **J3** | pre-flight STL validator | 30min | `scripts/validate_stl.py` (NEW) |
| **J4** | CLI --config 파일 | 1h | `cli/main.py` (~30 줄) |
| **J5** | GUI 키보드 shortcut | 30min | `desktop/qt_app/main_window.py` (~20 줄) |
| **J6** | 중앙 error catalog | 45min | `core/utils/error_catalog.py` (NEW) |
| **J7** | bench diff script | 30min | `scripts/bench_diff.py` (NEW) |
| **J8** | 최종 회귀 | 30min | (commit only) |

총 ~5 시간.

## 합격 기준

회귀 PASS, atomic edit, 각 카드 commit.
