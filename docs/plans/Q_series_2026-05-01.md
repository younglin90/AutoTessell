# Q-series — BETA2674+ (2026-05-01)

## Context

P-series (BETA2667-2673) 후 잔여 atomic 격차:
1. STL binary writer (현재 ASCII만 — binary 50% smaller).
2. Workspace cleanup utility.
3. CLI tier-test 명령 (single tier smoke test).
4. Mesh signed distance computation.
5. Triangle mesh edge-length stats.
6. Quality threshold 자동 적응 (env-gated).
7. Q-series tests.
8. Final regression.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **Q1** | STL binary writer | 30min | `core/utils/stl_writer.py` (~50 줄) |
| **Q2** | Workspace cleanup CLI | 30min | `cli/main.py` (~40 줄) |
| **Q3** | CLI tier-test 명령 | 45min | `cli/main.py` (~50 줄) |
| **Q4** | Triangle mesh signed distance | 1h | `core/utils/signed_distance.py` (NEW) |
| **Q5** | Edge length statistics | 30min | `core/analyzer/edge_stats.py` (NEW) |
| **Q6** | Auto threshold adapt | 30min | `core/generator/native_tet/quality.py` (~15 줄) |
| **Q7** | Q-series tests | 30min | `tests/test_q_series.py` (NEW) |
| **Q8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
