# N-series — BETA2653+ (2026-05-01)

## Context

M-series (BETA2647-2652) 후 잔여 atomic 격차:
1. STEP/IGES output via cadquery (CAD round-trip).
2. mesh quality plot script (matplotlib histogram).
3. parallel.py 통합 점검 (chunked Delaunay).
4. surface remesh + feature edges 통합.
5. ML inference benchmark.
6. CLI export 명령 12 포맷 분기.
7. M-series 통합 회귀.
8. 최종 회귀.

## Goal/Scope/Metric/Verify

- Goal: 7 카드 + 회귀 PASS (≥617).
- Scope: `core/utils/`, `cli/`, `tests/`, `scripts/`.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **N1** | matplotlib quality histogram | 45min | `scripts/plot_quality_histogram.py` (NEW) |
| **N2** | CLI export-mesh 12 포맷 분기 | 1h | `cli/main.py` (~50 줄) |
| **N3** | parallel.py audit log | 30min | `core/generator/native_tet/parallel.py` (~10 줄) |
| **N4** | ML inference benchmark | 45min | `scripts/bench_ml_inference.py` (NEW) |
| **N5** | surface remesh + feature edges | 30min | `core/preprocessor/native_remesh/__init__.py` (확장) |
| **N6** | Tier alias dump CLI | 30min | `cli/main.py` (~20 줄) |
| **N7** | N-series tests | 30min | `tests/test_n_series.py` (NEW) |
| **N8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
