# P-series — BETA2667+ (2026-05-01)

## Context

O-series (BETA2660-2666) 후 잔여 atomic 격차:
1. Geometry KPI report (bbox / volume / surface / Euler char).
2. Polyhedral mesh validator (face winding / non-convex check).
3. Tier alias bidirectional resolver helper.
4. CLI bench-summary 명령 (TSV → markdown).
5. Predictor confidence interval calc.
6. Volume mesh export filter (skip cells w/ q < threshold).
7. P-series tests.
8. Final regression.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **P1** | Geometry KPI report | 45min | `core/analyzer/geometry_kpi.py` (NEW) |
| **P2** | Polyhedral validator | 45min | `core/analyzer/polyhedral_check.py` (NEW) |
| **P3** | Tier alias resolver | 30min | `core/strategist/tier_selector.py` (~25 줄) |
| **P4** | CLI bench-summary | 30min | `cli/main.py` (~40 줄) |
| **P5** | Predictor confidence | 30min | `core/generator/native_ai/inference_batch.py` (~20 줄) |
| **P6** | Quality filter export | 45min | `scripts/export_filtered_quality.py` (NEW) |
| **P7** | P-series tests | 30min | `tests/test_p_series.py` (NEW) |
| **P8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
