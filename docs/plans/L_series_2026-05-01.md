# L-series — BETA2640+ (2026-05-01)

## Context

K-series (BETA2633-2639) 후 잔여 atomic 격차:
1. Abaqus .inp writer (FEM solver).
2. Mesh statistics CLI command.
3. Geometry feature extraction (sharp edges).
4. ML model versioning + checkpoint metadata.
5. PolyMesh diff utility (두 mesh 비교).
6. Doctor 명령 강화 (의존성 + 모델 + GPU 체크).
7. K-series test 통합 (회귀 보강).
8. 최종 회귀.

## Goal/Scope/Metric/Verify

- Goal: 7 카드 + 회귀 PASS (≥604).
- Scope: `core/utils/`, `cli/`, `tests/`.
- Metric: 카드 완료 수 + 회귀 PASS.
- Verify: 14 test suite pytest.

## 카드 (L1-L8)

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **L1** | Abaqus .inp writer | 1h | `core/utils/abaqus_writer.py` (NEW) |
| **L2** | CLI mesh-stats command | 45min | `cli/main.py` (~50 줄) |
| **L3** | Geometry feature edges | 45min | `core/analyzer/feature_edges.py` (NEW) |
| **L4** | ML model metadata + checksum | 30min | `scripts/train_quality_predictor.py` (~20 줄) |
| **L5** | polyMesh diff utility | 45min | `scripts/polymesh_diff.py` (NEW) |
| **L6** | Doctor 강화 (model/GPU 체크) | 30min | `cli/main.py` (~30 줄) |
| **L7** | K-series 통합 회귀 테스트 | 45min | `tests/test_k_series.py` (NEW) |
| **L8** | 최종 회귀 | 30min | (commit only) |

총 ~5 시간.
