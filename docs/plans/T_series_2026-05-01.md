# T-series — BETA2695+ (2026-05-01)

## Context

S-series (BETA2688-2694) 후 잔여 atomic 격차:
1. Surface normal smoothing (1-ring Laplacian).
2. Halton low-discrepancy seed sequence (volumetric).
3. Mesh I/O 종합 (NPY ↔ STL/OBJ 변환).
4. CLI export 의 native fmt 기본 binary 옵션.
5. bench result aggregator (multi JSON merge).
6. ML model registry (등록된 모델 관리).
7. T-series tests.
8. Final regression.

## 카드

| ID | 카드 | 시간 | 변경 파일 |
|----|------|------|----------|
| **T1** | Surface normal smoothing | 45min | `core/preprocessor/normal_smooth.py` (NEW) |
| **T2** | Halton seed sequence | 45min | `core/utils/halton.py` (NEW) |
| **T3** | NPY ↔ STL converter | 30min | `scripts/npy_to_stl.py` (NEW) |
| **T4** | bench result aggregator | 30min | `scripts/bench_aggregate.py` (NEW) |
| **T5** | ML model registry | 45min | `core/generator/native_ai/model_registry.py` (NEW) |
| **T6** | Native_tet boundary vertex stats | 30min | `core/analyzer/boundary_stats.py` (NEW) |
| **T7** | T-series tests | 30min | `tests/test_t_series.py` (NEW) |
| **T8** | 최종 회귀 | 30min | (commit only) |

총 ~5시간.
