# CARD BETA2256_VVV9F6_DRYRUN_ON (beta2256) — VVV9F #5 dry-run gate flip True (evidence emit, mesh discard)

**target_engine**: tet
**모티프**: Cheng-Dey-Edelsbrunner-Facello-Teng 1999 §4 Algo 4.1 step 1+2 — gate flip True for the dry-run wire installed in R190. Mesh untouched (helper return discard); the only behavioural delta is per-fid evidence log `native_tet_vvv9f_dryrun` carrying (n_samples, alpha, best_idx, best_min_q, wall_ms). R192 (apply card) will use this evidence to tune α/K and per-fid revert thresholds.

## 이론적 근거 (≤30줄)

- **문제 정의**: R190 wired the dry-run pair (`_perturb_weights_topK` + `_select_best_weight_assignment`) into VVV12 sliver_split block but with `_VVV9F_EXUDATION_DRYRUN=False`, so 0 evidence rows are emitted across 20 bench fids. Without per-fid evidence the next card (R192 mesh apply) cannot tune (top_k, alpha) safely.
- **핵심 단계** (≤2 line behaviour change):
  1. `mesher.py:2932` `_VVV9F_EXUDATION_DRYRUN: False → True`. Block already protected by `try/except` (R190) → exception path emits `native_tet_vvv9f_skipped`.
  2. **Wall safety guard (Cheng-Dey 1999 §4 Lemma 4.4 — only slivers benefit)**: gate further by `if _VVV9F_EXUDATION_DRYRUN and _n_sliver_pre >= 1:` → fids with 0 detected slivers (most bench cases per R185 `n_offplane_candidates=0` evidence) skip the helper entirely. Saves wall on no-sliver fids; emits evidence only where Cheng-Dey 1999 step-1 ω₀² weights are physically meaningful.
- **단조 보장**: helper return values discarded (mesh arrays bit-identical to R190). `final_pts`/`final_tets` not rebound. Only side effect = log line. Sliver-gate is a strict additional restriction (subset of unconditional gate) → still default-safe.
- **Wall cost estimate**: `_perturb_weights_topK` = O(M·6) Python loop, K=8 quality snapshots in select = O(K·M). For bench tet meshes (M ≤ ~5k typical), wall ≈ 30-150ms/fid. With sliver-gate, only fids with `_n_sliver_pre >= 1` pay (R185 evidence shows small subset). Total bench delta ≤ ~30s tolerance, well within 50s.
- **레퍼런스**: Cheng-Dey 1999 FOCS §4 Algo 4.1 (pp.291); `core/generator/native_tet/stellar.py:1206,1273`; R184 dry-run pattern `mesher.py:2908-2930`; R190 wire `mesher.py:2931-2953`.
- **혁신성**: novelty=1 (gate flip on existing wire), rigor=2 (sliver-gate + try/except + helper pure), impact=2 (R192 apply 직전 evidence 채널). 합=5 — sequence position OK.

## 변경

- 파일: `core/generator/native_tet/mesher.py` (단일 파일, ≤2줄 실효 변경)
- 함수: `_native_tet_pipeline` line ~2931-2953 (existing R190 dry-run block, inside VVV12 try-block)
- 핵심 변경 (정확히 2줄):
  1. line 2932 — `_VVV9F_EXUDATION_DRYRUN: bool = False` → `_VVV9F_EXUDATION_DRYRUN: bool = True` (comment update: `# R191/VVV9F #5 evidence emit (mesh discard, sliver-gated)`)
  2. line 2933 — `if _VVV9F_EXUDATION_DRYRUN:` → `if _VVV9F_EXUDATION_DRYRUN and _n_sliver_pre >= 1:` (sliver-gate safety guard, Cheng-Dey 1999 Lemma 4.4 의미 보존; `_n_sliver_pre` already in scope from line 2880)
- 단조 가드:
  - mesh untouched (R190 wire discards `_W`, `_bidx`, `_bmq` outputs).
  - sliver-gate guarantees no-op on `_n_sliver_pre==0` fids (most bench cases per R185).
  - helper pure (R188/R189 docstring asserts pts/tets read-only).
  - try/except path emits `native_tet_vvv9f_skipped` on any error → no crash propagation.
- AVOID 준수: AMIPS post-BSP 호출 / collapse_short_edges 패턴 미해당. neighbouring `_VVV9D_DRYRUN` (line 2909) 미건드림.

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (4 file 좁은 환경, env-fail 회피)
- bench 시간 ≤ 580 + 50s tolerance (wall safety guard 의도; R190 baseline 580.0s)
- tet metric: worst_mq 0.2069 ± 0.005, mean_q ± 0.005
- mesh arrays bit-identical to R190 (gate ON → log only, return discard)
- BL_OK 20/20 동등
- 로그 검증: `native_tet_vvv9f_dryrun` line 1+ emitted across sliver-positive fids (subset of 20 bench)

## 카드 시퀀스 위치

VVV9F (Cheng-Dey 1999 sliver exudation) sequence 5/6:
- #1 BETA2252_VVV9F2_DRYRUN_REVERT (R187, PASS — VVV9D revert)
- #2 BETA2253_VVV9F3_PERTURB_TOPK_HELPER (R188, PASS — helper #1)
- #3 BETA2254_VVV9F4_SELECT_BEST (R189, PASS — helper #2)
- #4 BETA2255_VVV9F5_DRYRUN_WIRE (R190, PASS — gate OFF)
- **#5 BETA2256_VVV9F6_DRYRUN_ON (R191, this card — gate ON + sliver-gate, mesh discard)**
- #6 BETA2257_VVV9F7_APPLY (R192, mesh apply with worst-mq revert guard, evidence-driven α/K tune)

다음 카드 (R192): BETA2257_VVV9F7_APPLY — actual mesh apply with per-fid worst-mq revert guard, α/K tuned by R191 evidence.
