# CARD BETA2268_VVV9I4_DIAG_ON (beta2268) — VVV9I #4 envelope distance diag gate ON (sliver-pre guarded)

**target_engine**: tet
**모티프**: fTetWild §3.2 envelope distance diagnostic gate flip — N×M KDTree+centroid 무게 회피용 sliver-pre>=1 guard

## 이론적 근거 (≤30줄)

- **문제 정의**: VVV9I #1-3 (R200/R201/R202) 가 envelope projection / centroid distance 헬퍼 + diag hook 까지 진입. 현재 `_VVV9I_DIAG=False` (mesher.py:3009) 로 evidence 미수집. fTetWild §3.2 envelope distance d(p, ∂Ω_ε) ≤ ε 가드를 다음 단계 (apply / project) 로 확장하려면 invasion histogram per-fid 수집이 선결.
- **본 카드의 핵심 아이디어**:
  1. mesher.py:3009 `_VVV9I_DIAG=False → True` (gate flip).
  2. KDTree + centroid distance 는 N(points)×M(faces) 비용 → bench 보호 위해 **sliver-pre>=1 guard** 추가 (R197 `_VVV9H_APPLY_DRYRUN` 와 동일 pattern, mesher.py:2983).
  3. 이중 가드: `if _VVV9I_DIAG and _n_sliver_pre >= 1:` — easy mesh (sliver=0, e.g. fid=100027) 는 skip, hard mesh (sliver≥1, fid=100030/100040) 만 측정.
- **수렴/안정성**: helper return discard (mesh 불변), 회귀 0. wall_ms log 로 cost 가시화.
- **레퍼런스**: Hu et al. 2020 fTetWild §3.2 (envelope distance), R197 sliver-pre guard pattern (mesher.py:2983).
- **혁신성 평가**: novelty 1 (gate flip), rigor 2 (guard 합리), impact 2 (다음 카드 apply 위한 invasion evidence). 합 5 — 시퀀스 가속용 정당.

## 변경

- 파일: `core/generator/native_tet/mesher.py` (단일)
- 위치: line 3009-3010 (VVV9I #3 try-block 내)
- 핵심 변경 (≤5줄):
  1. `_VVV9I_DIAG: bool = False` → `True`
  2. `if _VVV9I_DIAG:` → `if _VVV9I_DIAG and _n_sliver_pre >= 1:`
- 단조 가드: helper return discard (final_pts/final_tets 불변), sliver-pre guard 로 easy mesh wall_ms 0.

## 검증 명령

```bash
timeout 90 python3 -m pytest tests/test_native_tet_amips.py tests/test_native_tet_chunked.py tests/test_native_tet_cdt_recovery.py tests/test_native_tet_hausdorff.py -q
```

## 합격 기준

- 회귀 PASS (4 test files, 0 fail)
- worst_mq 0.208 ±0.005 stable (mesh ±0)
- bench ≤ R202 (66s) + 50s tolerance = ≤116s
- log emit `native_tet_vvv9i_diag` per-fid (sliver≥1 케이스만, n_invasion + max_dist + wall_ms 가시화)
- BL_OK 5/5

## 카드 시퀀스 위치

- VVV9I 시퀀스 (fTetWild §3.2 envelope distance) 의 #4 / 총 7 예상.
- 다음 카드: BETA2269_VVV9I5_INVASION_HARDEN — invasion threshold + max_dist 분포 분석 → 다음 step (project apply candidate selection) 가드 설계.

## AVOID 마커 준수

- single mesher.py 파일 ≤5줄 변경 (≤80줄 cap 준수).
- helper return discard (mesh 불변), Steiner / smooth_amips 호출 없음.
- bench tolerance +50s 충분 (sliver-pre guard 로 easy mesh wall_ms 0).
